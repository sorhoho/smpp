import { HaredoMessage } from './haredo-message';
import { Loggers } from './state';
export declare enum MessageManagerEvents {
    MESSAGE_MANAGER_DRAINED = "drained"
}
export declare const makeMessageManager: (logger: Loggers) => {
    length: () => number;
    isDrained: () => boolean;
    add: (message: HaredoMessage) => void;
    remove: (message: HaredoMessage) => void;
    drain: () => Promise<void>;
};
